apiVersion: v1
kind: Service
metadata:
  name: forex
  namespace: investments
  labels:
    app: forex
spec:
  selector:
    app: forex
  ports:
    - port: 8006
      targetPort: 8006
      name: http
  type: ClusterIP
