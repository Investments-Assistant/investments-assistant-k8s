apiVersion: v1
kind: Service
metadata:
  name: llm
  namespace: investments
  labels:
    app: llm
spec:
  selector:
    app: llm
  ports:
    - port: 11434
      targetPort: 11434
      name: http
  type: ClusterIP
