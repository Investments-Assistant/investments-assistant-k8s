apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: investments
  labels:
    app: gateway
spec:
  selector:
    app: gateway
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
