apiVersion: v1
kind: Service
metadata:
  name: news
  namespace: investments
  labels:
    app: news
spec:
  selector:
    app: news
  ports:
    - port: 8002
      targetPort: 8002
      name: http
  type: ClusterIP
