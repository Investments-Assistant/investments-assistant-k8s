apiVersion: v1
kind: Service
metadata:
  name: scheduler
  namespace: investments
  labels:
    app: scheduler
spec:
  selector:
    app: scheduler
  ports:
    - port: 8005
      targetPort: 8005
      name: http
  type: ClusterIP
